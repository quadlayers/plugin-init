{
	"name": "plugin-init",
	"version": "1.0.0",
	"description": "Plugin Init",
	"main": "index.js",
	"scripts": {
		"clean:build": "node ./bin/rm.js ./build",
		"clean:lock": "node ./bin/del.js ./composer.lock ./package-lock.json",
		"clean:dependencies": "node ./bin/rm.js ./vendor ./jetpack_vendor ./node_modules",
		"clean:zip": "node ./bin/cleanZip.js",
		"clean": "npm run clean:build & npm run clean:lock & npm run clean:dependencies & npm run clean:zip",
		"setup": "npm run clean & npm install --force && composer install && husky install && playwright install",
		"update": "npm update && composer update",
		"docker": "wp-env",
		"test:unit": "jest",
		"test:e2e": "npm run docker start && playwright test",
		"test": "npm run test:unit & npm run test:e2e",
		"start": "npm run clean:build && composer dump-autoload && wp-scripts start",
		"build": "npm run clean:build && composer dump-autoload -o &&  wp-scripts build",
		"build:zip": "npm run clean:zip && npm run build && node ./bin/build.js && node ./bin/buildZip.js"
	},
	"author": "QuadLayers",
	"license": "ISC",
	"devDependencies": {
		"@playwright/test": "^1.29.1",
		"@wordpress/env": "^5.8.0",
		"@wordpress/prettier-config": "^2.6.0",
		"@wordpress/scripts": "^23.6.0",
		"@wordpress/stylelint-config": "^21.6.0",
		"classnames": "^2.3.1",
		"css-loader": "^6.7.1",
		"depcheck": "^1.4.3",
		"eslint": "^8.30.0",
		"husky": "^8.0.2",
		"jszip": "^3.10.1",
		"lint-staged": "^13.1.0",
		"mini-css-extract-plugin": "^2.6.1",
		"node-sass-glob-importer": "^5.3.2",
		"prettier": "2.8.1",
		"sass-loader": "^13.0.2",
		"stylelint": "^14.16.0",
		"stylelint-config-standard": "^29.0.0",
		"webpack": "^5.74.0",
		"webpack-remove-empty-scripts": "^0.8.1"
	}
}
